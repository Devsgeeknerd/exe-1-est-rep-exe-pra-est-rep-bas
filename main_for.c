// Inclui bibliotecas necessárias.
#include <stdio.h>
#include <stdlib.h>

// Função principal.
void main()
{
    // Loop que itera de 0 a 10 (11 iterações).
    for (int l = 0; l < 11; l++)
    {
        // Imprime o valor de 'l' e a mensagem "TreinaWeb!".
        printf("%d TreinaWeb!\n", l);
    }

    // Retorna 0 indicando que a execução ocorreu sem erros.
    return 0;
}
